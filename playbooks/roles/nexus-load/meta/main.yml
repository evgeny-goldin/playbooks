---
dependencies:
  - { role: nexus,     when: periodic }
  - { role: gatling,   when: periodic }
  - { role: exhibitor, when: periodic,
                       maven_repo:        "{{ nexus_load.maven_repo }}",
                       exhibitor_service: false }
