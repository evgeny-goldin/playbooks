---
test_repo: {
  files_dir: /opt,
  simulations: [
    # { name: LightWarmup,  users: 1,   artifacts: few-files.txt },
    { name: Warmup,  users: 1,   artifacts: all-artifacts-size-sorted.txt },
    { name: Get10,   users: 10,  artifacts: all-artifacts-name-sorted.txt },
    { name: Get30,   users: 30,  artifacts: all-artifacts-name-sorted.txt },
    # { name: Get100,  users: 100, artifacts: all-artifacts-name-sorted.txt }
  ]
}
