ENABLED=true
HELIOS_AGENT_OPTS="--syslog --state-dir={{ helios.agent.state_dir }} --name={{ ansible_hostname }}.{{ domain }} --zk {{ helios_master }}:{{ zookeeper_port }} --service-registry http://{{ helios_master }}:{{ registry_port }} --service-registrar-plugin {{ registrar_plugin_jar }} --domain {{ domain }}"
