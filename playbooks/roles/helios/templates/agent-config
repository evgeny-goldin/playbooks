export DOCKER_HOST={{ helios.agent.docker.http }}
export ENABLED=true
export HELIOS_AGENT_OPTS="--state-dir={{ helios.agent.state_dir }} --name={{ agent_name }} --zk {{ helios_zk_connect }} --service-registry {{ registrar_plugin_endpoint }} --service-registrar-plugin {{ registrar_plugin_archive }} --domain {{ domain }}"
